-- ตัดสต๊อกของสำเร็จรูปตั้งแต่ตอนสั่ง ไม่ใช่ตอนปิดบิล
-- คอลัมน์นี้บอกว่าบรรทัดนั้นถูกหักออกจากสต๊อกไปแล้วเมื่อไหร่
-- บิลเก่าที่ยังเปิดค้างอยู่ตอนขึ้นระบบเป็น NULL ทั้งหมด = ยังไม่ได้หัก
-- แล้วจะไปหักตอนปิดบิลตามเดิม จึงไม่มีบรรทัดไหนถูกนับสองรอบ
ALTER TABLE `order_items`
  ADD COLUMN `stockDeductedAt` DATETIME(3) NULL;
