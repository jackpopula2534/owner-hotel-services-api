-- เลิกตัดสต๊อกเงียบเมื่อขายเกินของที่มี
--
-- เดิม: deductQty = Math.min(ที่ขาย, ที่มี) → ส่วนต่างหายไปไม่มีร่องรอย
--       สมุดเคลื่อนไหวกับยอดคงเหลือจึงกระทบยอดกันไม่ได้เลย
-- ใหม่: บันทึกจำนวนที่ขายจริงเต็มจำนวน + เก็บส่วนที่ขาดไว้ในคอลัมน์นี้
--       ยอดคงเหลือติดลบได้ = สัญญาณว่ามีของเข้าที่ไม่ได้ลงบันทึก ต้องไปตรวจนับ

ALTER TABLE `stock_movements`
  ADD COLUMN `shortfallQty` INT NOT NULL DEFAULT 0;
